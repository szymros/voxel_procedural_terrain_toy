use crate::chunk::Chunk;

pub struct Region {
    pub chunk_buffer: Vec<Chunk>,
    pub centre: [f32; 2],
}

// impl Region {
    // pub fn new(centre: [f32; 2]) -> Region {
        // let mut c: Vec<Chunk> = Vec::new();
        // for x in 0..3 {
            // for z in 0..3 {
                // c.push(Chunk::new([x as f32, 0.0, z as f 
        // }
        // Region {
            // chunk_buffer: c,
            // centre: centre,
        // }
// 
    // }
    // pub fn build_chunk_meshes(&mut self) {}
// }





